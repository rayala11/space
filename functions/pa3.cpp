//Write a C++ program that defines a function calculateSum to find the sum and another function calculateAverage to find the average of elements in an array of integers.
//In the main() function, ask the user to input the size of the array and its elements, then display the sum and average using these functions.
//ARRAYS NEW TOPIC, int a[5] use brackets for array
//array are always pass by referance
#include<iostream>
using namespace std;

int calculateSum(int l) {
    int sum=0;
    sum+=l;

}
int calculateAverage(int h) {
    int average =0; 
    average = 5/;
}

int main() {
    int arr[]={1,2,3,4,5};
    
    int sum = calculateSum(arr);
    int average = calculateAverage(arr);


}